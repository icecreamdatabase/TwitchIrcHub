namespace TwitchIrcHub.Authentication
{
    public struct AuthClaims
    {
        public const string AppId = nameof(AppId);
        public const string AppName = nameof(AppName);
        public const string AppKey = nameof(AppKey);
    }
}
